import BackButton from '../../components/BackButton';
import Layout from '../../components/layout';

const GroupDetail = () => {
  return <Layout leftChild={<BackButton />} hasNav={false}></Layout>;
};

export default GroupDetail;
